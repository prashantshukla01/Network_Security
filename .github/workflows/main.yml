name: workflow

on:
    push:
        branches:
            - main
        path-ignore:
            - 'README.md'
jobs:
    integration:
        name: Continueous Integration
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Code
              uses: actions/checkout@v3

            - name: Lint Code
              run: echo "Linting code"

            - name: Run unit tests
              run: echo "Running unit tests"
